---
import BaseLayout from '../../layouts/BaseLayout.astro';
import { getHaarlemMovies, getTielMovies, getUpcomingMovies } from '../../lib/sheets';
import { getLangFromUrl, useTranslations, getLocalizedPath } from '../../i18n/utils';

const lang = getLangFromUrl(Astro.url);
const t = useTranslations(lang);

const haarlemUpcoming = getUpcomingMovies(getHaarlemMovies());
const tielUpcoming = getUpcomingMovies(getTielMovies());
---

<BaseLayout title={t('schedule.title')} description="Bekijk het programma van Nelsons Film - gratis openluchtbioscoop op meerdere locaties">
  <section class="schedule-page section">
    <div class="container">
      <header class="page-header">
        <h1>{t('schedule.title')}</h1>
        <p class="page-subtitle">{t('schedule.chooseLocation')}</p>
        <p class="page-info">{t('schedule.selectionInfo')}</p>
        <p class="page-free">{t('schedule.freeInfo')}</p>
      </header>

      <div class="locations-grid">
        <a href={getLocalizedPath('/programma/haarlem', lang)} class="location-tile">
          <span class="tile-icon">📍</span>
          <div class="tile-content">
            <h2 class="tile-city">{t('schedule.locationHaarlem')}</h2>
            <span class="tile-count">{haarlemUpcoming.length} {t('schedule.activeScreenings')}</span>
          </div>
          <span class="tile-badge">{lang === 'nl' ? 'Actief' : 'Active'}</span>
          <span class="tile-arrow">→</span>
        </a>

        <a href={getLocalizedPath('/programma/tiel', lang)} class="location-tile">
          <span class="tile-icon">📍</span>
          <div class="tile-content">
            <h2 class="tile-city">{t('schedule.locationTiel')}</h2>
            <span class="tile-count">{tielUpcoming.length} {t('schedule.activeScreenings')}</span>
          </div>
          <span class="tile-badge">{lang === 'nl' ? 'Actief' : 'Active'}</span>
          <span class="tile-arrow">→</span>
        </a>
      </div>

      <!-- Coming Soon Section -->
      <div class="coming-soon-section">
        <div class="coming-soon-content">
          <div class="coming-soon-icon">🌍</div>
          <h2>{t('schedule.expandingTitle')}</h2>
          <p>{t('schedule.expandingText')}</p>
          <a href={getLocalizedPath('/voor-gemeenten', lang)} class="btn btn-primary">
            {t('schedule.expandingCta')}
          </a>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  .schedule-page {
    min-height: 80vh;
  }

  .page-header {
    text-align: center;
    margin-bottom: var(--space-xl);
  }

  .page-subtitle {
    color: var(--color-text-muted);
    font-size: 1.125rem;
    margin-top: var(--space-sm);
  }

  .page-info {
    color: var(--color-text-muted);
    font-size: 1rem;
    margin-top: var(--space-md);
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .page-free {
    color: var(--color-accent);
    font-size: 0.95rem;
    font-weight: 500;
    margin-top: var(--space-sm);
  }

  .locations-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(320px, 1fr));
    gap: var(--space-lg);
    max-width: 800px;
    margin: 0 auto var(--space-xl);
  }

  .location-tile {
    display: flex;
    align-items: center;
    gap: var(--space-md);
    padding: var(--space-lg) var(--space-xl);
    background: linear-gradient(135deg, rgba(233, 69, 96, 0.1), rgba(244, 162, 97, 0.05));
    border: 1px solid rgba(233, 69, 96, 0.3);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--color-text);
    transition: all var(--transition-normal);
  }

  .location-tile:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.3);
    border-color: var(--color-accent);
  }

  .tile-icon {
    font-size: 2rem;
  }

  .tile-content {
    flex: 1;
  }

  .tile-city {
    font-size: 1.5rem;
    font-weight: 700;
    margin-bottom: 4px;
  }

  .tile-count {
    font-size: 0.85rem;
    color: var(--color-text-muted);
  }

  .tile-badge {
    font-size: 0.7rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    background: var(--color-accent);
    color: white;
    padding: 4px 10px;
    border-radius: var(--radius-sm);
  }

  .tile-arrow {
    font-size: 1.5rem;
    color: var(--color-text-muted);
    transition: transform var(--transition-fast);
  }

  .location-tile:hover .tile-arrow {
    transform: translateX(4px);
    color: var(--color-accent);
  }

  /* Coming Soon Section */
  .coming-soon-section {
    margin-top: var(--space-xl);
    padding: var(--space-xl);
    background: linear-gradient(135deg, rgba(255, 255, 255, 0.02), rgba(255, 255, 255, 0.05));
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-lg);
    text-align: center;
  }

  .coming-soon-content {
    max-width: 600px;
    margin: 0 auto;
  }

  .coming-soon-icon {
    font-size: 3rem;
    margin-bottom: var(--space-md);
  }

  .coming-soon-section h2 {
    margin-bottom: var(--space-md);
    color: var(--color-text);
  }

  .coming-soon-section p {
    color: var(--color-text-muted);
    line-height: 1.7;
    margin-bottom: var(--space-lg);
  }

  @media (max-width: 768px) {
    .locations-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
